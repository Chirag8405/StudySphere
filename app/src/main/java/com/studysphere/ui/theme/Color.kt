package com.studysphere.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Shadcn Inspired Palette ────────────────────────────────────────────────

val PrimaryPurple = Color(0xFF7F22FE)

// Light Mode (Pure White Base)
val PureWhite   = Color(0xFFFFFFFF)
val Gray50      = Color(0xFFF9FAFB)
val Gray100     = Color(0xFFF3F4F6)
val Gray200     = Color(0xFFE5E7EB)
val Gray300     = Color(0xFFD1D5DB)
val Gray400     = Color(0xFF9CA3AF)
val Gray500     = Color(0xFF6B7280)
val Gray600     = Color(0xFF4B5563)
val Gray700     = Color(0xFF374151)
val Gray800     = Color(0xFF1F2937)
val Gray900     = Color(0xFF111827)
val PureBlack   = Color(0xFF000000)

// Dark Mode (Pure Black Base)
val DarkBg      = Color(0xFF000000)
val DarkSurface = Color(0xFF09090B)
val DarkCard    = Color(0xFF121214)
val DarkBorder  = Color(0xFF27272A)
val DarkMuted   = Color(0xFFA1A1AA)

// Semantic (Vibrant but clean)
val Green500   = Color(0xFF22C55E)
val Amber500   = Color(0xFFEAB308)
val Red500     = Color(0xFFEF4444)
val Red600     = Color(0xFFDC2626)

// Priority colors
val PriorityLow    = Color(0xFF94A3B8) // Slate
val PriorityMedium = Amber500
val PriorityHigh   = Red500

// Subject palette for auto-assignment (Now neutral/monochrome for consistency)
val SubjectColors = listOf(
    "#18181B", // Zinc 900
    "#27272A", // Zinc 800
    "#3F3F46", // Zinc 700
    "#52525B", // Zinc 600
    "#71717A", // Zinc 500
    "#7F22FE", // Primary
)
